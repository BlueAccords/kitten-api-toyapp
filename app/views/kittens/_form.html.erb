<%= form_for @kitten do |f| %>
	<% if @kitten.errors.any? %>
    	<div id="error_explanation">
      		<h2><%= pluralize(@kitten.errors.count, "error") %> prohibited this kitten from being saved:</h2>

      		<ul>
      			<% @kitten.errors.full_messages.each do |message| %>
        			<li><%= message %></li>
     			<% end %>
      		</ul>
    	</div>
  	<% end %>


	<%= f.label :name %>
	<%= f.text_field :name %> <br/>
	<%= f.label :age %>
	<%= f.number_field :age %> <br/>
	<%= f.label :cuteness %>
	<%= f.number_field :cuteness, within: 1..5 %> <br/>
	<%= f.label :softness %>
	<%= f.number_field :softness, within: 1..5 %> <br/>
	<%= f.submit "Submit Info" %>
<% end %>